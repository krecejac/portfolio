DB_HOST=localhost
DB_PORT=5432
DB_USER=surf
DB_PASSWORD=
DB_NAME=surf
JWT_SECRET=
# Windy Webcams API key (separate from the Map Forecast key) — powers /api/cams.
# Register a free key at https://api.windy.com/keys (Webcams API).
WINDY_WEBCAMS_KEY=
# SMTP for account-verification emails. Leave SMTP_HOST empty in dev — the
# verification link is logged to the server console instead of emailed.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Swell <no-reply@swell.app>